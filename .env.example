# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Better Auth
# Secret used by Better Auth
BETTER_AUTH_SECRET=""

# URL pública de la app. Normalmente se deja VACÍA: en local se asume
# localhost:3000 y en Vercel se deduce sola del dominio del proyecto.
# Ponla solo si usas un dominio propio, p. ej. https://finanzas.roborregos.com
BETTER_AUTH_URL=""

# Proveedores sociales (opcionales). Cada uno se activa solo si su par de
# credenciales está completo; si los dejas vacíos queda solo email/contraseña.
BETTER_AUTH_GITHUB_CLIENT_ID=""
BETTER_AUTH_GITHUB_CLIENT_SECRET=""

BETTER_AUTH_GOOGLE_CLIENT_ID=""
BETTER_AUTH_GOOGLE_CLIENT_SECRET=""

# Supabase Postgres (Prisma)
# Cópialas del dashboard de Supabase:
#   Project Settings -> Database -> Connection string -> pestaña "ORMs"
#
# DATABASE_URL: pooled, Supavisor en modo transacción (puerto 6543). La usa la app
# en runtime. `pgbouncer=true` le dice a Prisma que apague los prepared statements,
# que no sobreviven a un pool en modo transacción.
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
#
# DIRECT_URL: conexión directa en modo sesión (puerto 5432). Solo la usan
# `prisma migrate` y `prisma db push`; PgBouncer no soporta ese DDL.
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@<region>.pooler.supabase.com:5432/postgres"
